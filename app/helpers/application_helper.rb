module ApplicationHelper
    # def current_user
    #     @current_user ||= Student.find(session[:student_id]) if session[:student_id]
    # end

    # def logged_in?
    #     !!current_user
    # end

    # def session_link
    #     if (logged_in?)
    #         link_to('Log out', logout_path, method: :DELETE)
    #     else
    #         link_to('Log in', login_path)
    #         # link_to('Sign up', 'students/new')
    #     end
    # end
end
